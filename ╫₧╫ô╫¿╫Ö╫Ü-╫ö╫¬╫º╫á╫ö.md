# מדריך התקנה: תיבת הדילמות

המדריך הזה מיועד למי שלא כתב קוד מעולם. הוא לוקח בערך שעתיים, רובן המתנה לאישורים. בסופו תהיה לך מערכת חיה בכתובת משלה, שכל מתנדב פותח בדפדפן בטלפון.

**מה מקימים, בקצרה:** הקוד של המערכת יושב ב-GitHub (ארכיון קוד). Vercel לוקח את הקוד משם ומריץ אותו באינטרנט. Supabase מחזיק את מסד הנתונים (משתמשים, שאלות, תשובות). 019 שולח SMS. Resend שולח מיילים. cron-job.org "מצלצל" למערכת כל חצי שעה כדי שתשלח תזכורות. כל השירותים חינמיים בהיקף שלנו, למעט ה-SMS שעולה אגורות להודעה.

**מה צריך להכין:** כתובת מייל, כרטיס אשראי (רק ל-019, לרכישת חבילת SMS), והקובץ `tiba-src.zip` שקיבלת.

---

## שלב 1: הקוד עולה ל-GitHub (10 דקות)

1. היכנס ל-[github.com](https://github.com) ופתח חשבון (Sign up). אמת את המייל.
2. לחץ על הפלוס (+) למעלה מימין → **New repository**.
3. שם: `tiba`. סמן **Private**. אל תסמן שום דבר אחר. לחץ **Create repository**.
4. בעמוד שנפתח לחץ על הקישור **uploading an existing file**.
5. במחשב שלך, חלץ את `tiba-src.zip`. תיווצר תיקייה בשם `tiba`. פתח אותה, סמן **את כל מה שבתוכה** (לא את התיקייה עצמה), וגרור לחלון הדפדפן, לאזור "Drag files here". חכה שכל הקבצים יעלו (יש כ-60).
6. למטה לחץ **Commit changes**.

> אם הגרירה של תיקיות לא עובדת בדפדפן שלך: התקן את [GitHub Desktop](https://desktop.github.com), בחר File → Add local repository → התיקייה `tiba` → Publish repository (סמן "Keep this code private").

---

## שלב 2: מסד הנתונים ב-Supabase (10 דקות)

1. היכנס ל-[supabase.com](https://supabase.com) → **Start your project** → הירשם עם GitHub (הכי קל).
2. **New project**. שם: `tiba`. סיסמת מסד נתונים: לחץ Generate ושמור אותה במקום בטוח (לא נצטרך אותה כרגע, אבל שמור). Region: **Frankfurt (eu-central-1)**, הכי קרוב לישראל. **Create new project**. חכה כדקה.
3. בתפריט השמאלי לחץ על **SQL Editor** (אייקון של דף עם ">_").
4. פתח במחשב את הקובץ `supabase/schema.sql` מתוך התיקייה `tiba` (בפנקס רשימות / TextEdit), העתק את כל התוכן, הדבק בחלון ה-SQL, ולחץ **Run** (או Ctrl+Enter). למטה יופיע "Success. No rows returned". זהו, הטבלאות נוצרו.
5. עכשיו נאסוף שני ערכים. בתפריט השמאלי: **Project Settings** (גלגל שיניים למטה) → **API** (או **API Keys**):
   - **Project URL**: כתובת שנראית כמו `https://abcdefgh.supabase.co`. העתק ושמור בשם `SUPABASE_URL`.
   - **service_role** (בחלק "Project API keys", לחץ Reveal; בממשק החדש זה נקרא **Secret key**): מחרוזת ארוכה. העתק ושמור בשם `SUPABASE_SERVICE_ROLE_KEY`. **זה מפתח סודי**. לא לשלוח לאף אחד ולא להדביק בשום מקום חוץ מ-Vercel.

---

## שלב 3: המערכת עולה לאוויר ב-Vercel (15 דקות)

1. היכנס ל-[vercel.com](https://vercel.com) → **Sign Up** → **Continue with GitHub**. אשר את החיבור.
2. **Add New… → Project**. תראה את הרשימה של ה-repositories שלך ב-GitHub. ליד `tiba` לחץ **Import**. (אם לא רואים אותו: לחץ "Adjust GitHub App Permissions" ותן גישה ל-tiba.)
3. לפני שלוחצים Deploy: פתח את **Environment Variables** והוסף שורה אחרי שורה (Key בצד שמאל, Value בצד ימין, ואז Add):

   | Key | Value |
   |---|---|
   | `SUPABASE_URL` | הכתובת מ-Supabase |
   | `SUPABASE_SERVICE_ROLE_KEY` | המפתח הסודי מ-Supabase |
   | `AUTH_SECRET` | מחרוזת אקראית ארוכה. פתח [generate-secret.vercel.app/32](https://generate-secret.vercel.app/32) והעתק מה שמופיע |
   | `CRON_SECRET` | עוד מחרוזת אקראית, רענן את אותו דף והעתק שוב |
   | `MANAGER_PHONE` | מספר הנייד שלך, בפורמט `05XXXXXXXX` בלי מקפים |
   | `SMS_PROVIDER` | `log` (בינתיים. נשנה ל-019 בשלב 5) |
   | `APP_URL` | `https://tiba.vercel.app` (נתקן אחרי הפריסה) |
   | `REMIND_AFTER_HOURS` | `2` |
   | `ESCALATE_AFTER_HOURS` | `24` |

4. לחץ **Deploy**. זה לוקח כ-2 דקות. בסוף תראה קונפטי וכתובת בסגנון `tiba-xxxx.vercel.app`.
5. לחץ על הכתובת. אמור להיפתח מסך הכניסה של תיבת הדילמות. הכנס את מספר הטלפון שלך (זה שב-`MANAGER_PHONE`) ולחץ "שלח לי קוד". מכיוון שאנחנו במצב `log`, הקוד מופיע על המסך במקום ב-SMS. הכנס אותו. אתה בפנים, כמנהל האזור.
6. חזור ל-Vercel → הפרויקט → **Settings → Environment Variables**. ערוך את `APP_URL` לכתובת האמיתית שקיבלת (למשל `https://tiba-xxxx.vercel.app`, בלי `/` בסוף). שמור. אחר כך **Deployments** → שלוש נקודות ליד הפריסה האחרונה → **Redeploy**. (הכתובת הזאת נכנסת לקישורים שבהודעות, לכן היא חייבת להיות נכונה.)

> רוצה כתובת יפה כמו `tiba.hatzalah-sharon.org.il`? אפשר לחבר דומיין משלך ב-Settings → Domains. לא חובה.

---

## שלב 4: הגדרות ראשונות במערכת (5 דקות)

1. במערכת, לחץ **הגדרות** (בכותרת) ומלא את השם שלך והמייל שלך.
2. לחץ **ניהול → משתמשים**. ב"הוספת משתמש ידנית" הוסף כל אחד מהעונים המוסמכים: שם, טלפון, תפקיד "עונה מוסמך/ת". הם יוכלו להיכנס מיד בלי לחכות לאישור.
3. אפשר גם להוסיף מתנדבים ידנית, אבל אין צורך: מתנדב שנכנס בפעם הראשונה נרשם בעצמו (שם, צוות, מייל), אתה מקבל הודעה, ומאשר אותו במסך הזה בלחיצה אחת.

---

## שלב 5: SMS אמיתי דרך 019 (30 דקות + המתנה לאישור)

1. היכנס ל-[019sms.co.il](https://www.019sms.co.il) ופתח חשבון עסקי. תתבקש לפרטי הארגון. רכוש חבילת הודעות (חבילה של כמה אלפי הודעות מספיקה לשנה: כל שאלה שולחת הודעה אחת לכל עונה, ואחת לשואל כשיש תשובה).
2. בקש **שם שולח** (Sender ID), למשל `TibaEMS` או `UH-Sharon`. באנגלית, עד 11 תווים. האישור לוקח בדרך כלל יום עסקים.
3. באתר 019: **הגדרות → ניהול טוקנים (API Token)** → צור טוקן חדש. **העתק אותו מיד**, הוא מוצג פעם אחת בלבד.
4. חשוב: אם בהגדרות החשבון יש אפשרות "בדיקת כתובת IP מורשית", ודא שהיא **כבויה**. ל-Vercel אין כתובת קבועה.
5. ב-Vercel → Settings → Environment Variables:
   - `SMS_PROVIDER` ← שנה ל-`019`
   - הוסף `SMS_019_USERNAME` ← שם המשתמש שלך ב-019
   - הוסף `SMS_019_TOKEN` ← הטוקן
   - הוסף `SMS_SENDER` ← שם השולח שאושר
6. **Redeploy** (כמו בשלב 3.6).
7. בדיקה: פתח את המערכת בחלון גלישה פרטית, הכנס את הטלפון שלך. הפעם הקוד צריך להגיע ב-SMS אמיתי. אם לא הגיע: **ניהול → יומן התראות** מציג כל שליחה, ואם נכשלה, למה.

> אם אתם עובדים עם InforUMobile במקום 019: `SMS_PROVIDER=inforu`, ו-`SMS_INFORU_USERNAME` + `SMS_INFORU_TOKEN`. הקוד למקרה הזה מבוסס על ה-API הציבורי שלהם ולא נבדק מולם, אז אם יש כישלון ביומן, שלח לי את הודעת השגיאה ונתקן.

---

## שלב 6: מיילים דרך Resend (15 דקות, אופציונלי)

ה-SMS הוא ערוץ ההתראות המרכזי. מייל הוא תוספת נוחה למי שרוצה. אפשר לדלג ולחזור לכאן אחר כך.

1. היכנס ל-[resend.com](https://resend.com) והירשם. החבילה החינמית: 3,000 מיילים בחודש, יותר ממספיק.
2. **חשוב לדעת:** בלי דומיין מאומת, Resend שולח מיילים רק לכתובת שלך. כדי לשלוח לכל המתנדבים צריך דומיין (למשל דומיין של הסניף, או דומיין זול שקונים ב-Namecheap/GoDaddy בכ-10 דולר לשנה). ב-Resend: **Domains → Add Domain**, ואז מוסיפים 3 רשומות DNS אצל ספק הדומיין לפי ההוראות שמופיעות. זה החלק היחיד במדריך שדורש קצת סבלנות.
3. **API Keys → Create API Key** → העתק.
4. ב-Vercel הוסף: `RESEND_API_KEY` ← המפתח, ו-`EMAIL_FROM` ← `תיבת הדילמות <tiba@הדומיין-שלך>`. Redeploy.

---

## שלב 7: תזכורות אוטומטיות (5 דקות)

המערכת יודעת לשלוח תזכורת לעונים אם שאלה לא נלקחה תוך שעתיים, ולהתריע לך אם שאלה פתוחה 24 שעות. בשביל זה מישהו צריך "להעיר" אותה כל כמה דקות:

1. היכנס ל-[cron-job.org](https://cron-job.org) והירשם (חינם).
2. **Create cronjob**. Title: `tiba reminders`. URL:
   `https://הכתובת-שלך.vercel.app/api/cron/reminders?secret=הערך-של-CRON_SECRET`
3. Schedule: **Every 30 minutes**. שמור.
4. לחץ על "Test run" או חכה חצי שעה. בהיסטוריה של הג'וב אמורה להופיע תשובה `{"ok":true,...}`.

---

## שלב 8: להפיץ למתנדבים

שלח לקבוצת הוואטסאפ של האזור הודעה בסגנון:

> נפתחה **תיבת הדילמות**: מקום לשאול שאלות מקצועיות אחרי אירוע ולקבל תשובה מעונה מוסמך. הכניסה עם הטלפון שלכם, בלי סיסמה: הכתובת-שלך.vercel.app
> אחרי הכניסה הראשונה ממלאים שם וצוות, ואני מאשר.
> כדי שזה יראה כמו אפליקציה: באייפון, ספארי → כפתור שיתוף → "הוסף למסך הבית". באנדרואיד, כרום → שלוש נקודות → "הוסף למסך הבית".

בימים הראשונים תיכנס ל**ניהול → משתמשים** כמה פעמים ביום ותאשר את הממתינים (תקבל SMS על כל הרשמה).

---

## תפעול שוטף

**מה זה עולה.** GitHub, Vercel, Supabase, Resend ו-cron-job.org חינמיים בהיקף שלנו. Supabase משהה פרויקטים חינמיים שלא היו בשימוש שבוע; המערכת "מעירה" אותו אוטומטית בכניסה הראשונה, וגם ה-cron של התזכורות שומר אותו ער. ה-SMS: כ-0.05–0.10 ש"ח להודעה, כלומר בערך 150 ש"ח לשנה בקצב של 10 שאלות בשבוע ו-5 עונים.

**מי רואה מה.** מתנדב רואה רק את השאלות שלו ואת מאגר הידע. עונה רואה את כל התור. מנהל רואה הכל כולל דוח, משתמשים ויומן התראות. מסד הנתונים סגור לחלוטין מבחוץ; רק השרת ניגש אליו.

**כשמשהו לא עובד.** הכלי הכי מהיר: פתח בדפדפן את `הכתובת-שלך.vercel.app/api/health`. תראה מה מוגדר ומה לא, והאם החיבור למסד הנתונים תקין (אם לא, ההודעה תגיד אם הבעיה במפתח או שהסכמה לא הורצה). הכלי השני הוא **ניהול → יומן התראות**, שמראה כל SMS ומייל ומה קרה איתם. הכלי השני הוא Vercel → הפרויקט → **Logs**, שם רואים שגיאות של השרת. אם משהו נשבר אחרי שינוי הגדרות, בדוק ש-Redeploy נעשה אחרי השינוי.

**גיבוי.** Supabase → Database → Backups עושה גיבוי יומי אוטומטי (בחבילה החינמית נשמר גיבוי של 7 ימים). לגיבוי ידני: Table Editor → questions → Export to CSV.

**שינויים.** כל שינוי בקוד (ניסוחים, קטגוריות, צוותים, כללי התראות) הוא עדכון של קובץ ב-GitHub. Vercel פורס אוטומטית תוך דקה. הקטגוריות והצוותים נמצאים בקובץ `lib/types.ts`; ניסוחי ההודעות ב-`app/actions.ts`; שעות התזכורת בהגדרות הסביבה ב-Vercel, בלי לגעת בקוד.

**וואטסאפ.** שליחה יזומה בוואטסאפ מחייבת חשבון WhatsApp Business API ותבניות הודעה מאושרות אצל מטא, תהליך של כמה שבועות. המערכת מוכנה לזה (`WHATSAPP_PROVIDER=twilio` עם פרטי Twilio), אבל אני ממליץ להתחיל עם SMS ומייל, ולפתוח את הוואטסאפ רק אם המתנדבים יבקשו.

---

## רשימת המשתנים המלאה (לעיון)

| משתנה | מה זה | חובה |
|---|---|---|
| `APP_URL` | כתובת המערכת, בלי `/` בסוף | כן |
| `AUTH_SECRET` | מחרוזת אקראית לחתימת כניסות | כן |
| `SUPABASE_URL` | כתובת הפרויקט ב-Supabase | כן |
| `SUPABASE_SERVICE_ROLE_KEY` | מפתח סודי של Supabase | כן |
| `MANAGER_PHONE` | הטלפון של המנהל הראשון | כן |
| `CRON_SECRET` | סוד לנקודת התזכורות | כן |
| `SMS_PROVIDER` | `019` / `inforu` / `twilio` / `log` | כן |
| `SMS_SENDER` | שם השולח שאושר | ל-019/inforu |
| `SMS_019_USERNAME`, `SMS_019_TOKEN` | פרטי 019 | ל-019 |
| `SMS_INFORU_USERNAME`, `SMS_INFORU_TOKEN` | פרטי Inforu | ל-inforu |
| `RESEND_API_KEY`, `EMAIL_FROM` | מייל | לא (בלי זה אין מיילים) |
| `REMIND_AFTER_HOURS` | תזכורת לעונים אחרי X שעות | לא (ברירת מחדל 2) |
| `ESCALATE_AFTER_HOURS` | התראה למנהל אחרי X שעות | לא (ברירת מחדל 24) |
| `WHATSAPP_PROVIDER` | `off` / `twilio` | לא |
| `TWILIO_*` | פרטי Twilio | רק אם משתמשים |
